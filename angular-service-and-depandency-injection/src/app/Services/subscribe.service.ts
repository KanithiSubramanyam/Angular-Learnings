import { Injectable } from "@angular/core";

@Injectable()   
export class SubscribeService {

    OnSubscribeClicked(){
        //Add User to DB
    
        // Send mail with subscription detail
    
        // allow user to the services
    
        alert("Subscribed Successfully");
      }
}